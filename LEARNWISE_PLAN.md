# LearnWise — الحزمة الكاملة للمشروع
السيناريو الكامل + قاعدة البيانات + خطة التنفيذ المرحلية

---

# الجزء الأول — سيناريو المشروع الكامل

## 1. المشكلة
مع الانتشار الهائل للمحتوى التعليمي المجاني على الإنترنت (فيديوهات، مقالات)، يواجه المتعلمون صعوبة في إيجاد محتوى مناسب لمستواهم واهتماماتهم من بين كمية هائلة من الخيارات، مما يؤدي لإضاعة الوقت أو الاعتماد على محتوى غير ملائم.

## 2. الهدف العام
بناء منصة ويب ذكية باسم **LearnWise** (Next.js full-stack) تجمع محتوى تعليمي (فيديوهات ومقالات) في مجال البرمجة ولغات البرمجة بشكل مبدئي، مع إمكانية توسيع المجالات مستقبلاً، وتوصي كل مستخدم بمحتوى مخصص باستخدام خوارزميات ذكاء اصطناعي تعمل محلياً بالكامل (offline) بعد مرحلة الإعداد الأولى.

## 3. المستخدم المستهدف
أي شخص مهتم بالتعلم ضمن المهارات أو المجالات المتوفرة على المنصة. بما أنها مصممة لتكون قابلة للتوسع مستقبلياً، فإن المستخدم المستهدف غير مقتصر على مجال واحد في التصميم العام، رغم أن النسخة الأولية (MVP) الحالية مركّزة على البرمجة ولغات البرمجة.

## 4. خريطة الموقع (Sitemap)

### القسم العام — بدون تسجيل دخول
- الرئيسية (Landing) — تعريف بالمنصة + عرض عينة محدودة من المحتوى (Teaser): صورة وعنوان فقط، بدون تفاصيل أو رابط فعلي
- من نحن / عن المنصة
- ثقة الطلاب (إحصائيات عامة)
- الخدمات — شرح آلية عمل النظام الذكي
- الأسئلة الشائعة
- تواصل معنا
- زر تسجيل / دخول (ثابت في شريط التنقل)

### القسم المقفول — يتطلب تسجيل دخول
- قسم الكورسات / المحتوى التعليمي بالكامل — أي محاولة دخول من زائر غير مسجل تُعاد توجيهها لصفحة التسجيل

### قسم الطالب — بعد تسجيل الدخول
- اللوحة الرئيسية الموحدة (نظرة عامة + سجل النشاط + الإعدادات)
- صفحة "اقترح مجال جديد"

### قسم الأدمن — منفصل بالكامل
- تسجيل دخول أدمن (مسار وتصميم منفصلان تماماً عن الطالب)
- لوحة تحكم كاملة (إحصائيات + CRUD محتوى + CRUD مستخدمين + مراجعة اقتراحات الطلاب)

## 5. توثيق المسارات (User Journeys)

### المسار الأول: الزائر غير المسجل
| # | الخطوة | التفاصيل |
|---|---|---|
| 1 | دخول الموقع | يشاهد الصفحة الرئيسية بحرية كاملة |
| 2 | تصفح الصفحات العامة | من نحن، الخدمات، ثقة الطلاب، الأسئلة الشائعة، تواصل معنا |
| 3 | مشاهدة عينة محتوى (Teaser) | كروت محدودة: صورة وعنوان فقط، بدون تفاصيل أو رابط فعلي |
| 4 | محاولة دخول قسم الكورسات | إعادة توجيه فورية لرسالة: سجّل دخول لتصفح المحتوى الكامل |
| 5 | نقطة القرار | تسجيل حساب (ينتقل لمسار الطالب) أو مغادرة الموقع |

### المسار الثاني: الطالب المسجل
| # | الخطوة | التفاصيل |
|---|---|---|
| 1 | التسجيل | فورم بسيط: اسم، إيميل، كلمة سر ← إنشاء حساب |
| 2 | انتقال تلقائي فوري | بدون أي صفحة وسيطة، مباشرة إلى Onboarding |
| 3 | Onboarding — 6 أسئلة | المستوى مع الخبرة السابقة (سؤال مدمج)، المواضيع المهتم بها، الهدف من التعلم، أسلوب التعلم (عملي أم نظري)، الوقت المتاح أسبوعياً، نوع المحتوى المفضل |
| 4 | بناء البروفايل الذكي | حفظ الإجابات وحساب متجه أولي (initial_embedding) — يحل مشكلة cold-start |
| 5 | شاشة تجهيز | شريط تقدم دائري بنسبة مئوية (0% → 100%) مع نص "جاري إعداد المحتوى الخاص بك..." |
| 6 | دخول المنصة | زر "ادخل للمنصة" يصبح فعّالاً بعد الاكتمال ← اللوحة الرئيسية بمحتوى وتوصيات جاهزة |

**ملاحظة على استخدام الإجابات:** المستوى مع الخبرة السابقة، المواضيع، الهدف، وأسلوب التعلم (عملي/نظري) تُستخدم مباشرة في بناء المتجه الأولي (Embedding). أما الوقت المتاح أسبوعياً ونوع المحتوى المفضل فتُستخدم **كفلاتر** تُطبَّق على نتائج الترشيح بعد حسابها، وليست جزءاً من حساب التشابه الدلالي نفسه.

**بعد الدخول:** زر تسجيل الدخول في شريط التنقل يتحول إلى اسم الطالب وصورته الرمزية، والضغط عليه يفتح تفاصيل حسابه. اللوحة الرئيسية موحدة بتبويبات:
- نظرة عامة (توصيات + استكشاف المواضيع)
- سجل النشاط (مشاهدة + بحث + أوقات دخول)
- الإعدادات (بروفايل + تعديل الأفضليات الستة)

**دورة الاستخدام المستمرة:** مشاهدة محتوى ← فتح الرابط الأصلي (يوتيوب / Dev.to) ← تحديث سجل المشاهدة ← تحسّن التوصيات القادمة عبر تحديث `current_embedding` (معادلة EMA)، وهكذا بشكل متكرر.

**ميزة إضافية:** صفحة "اقترح مجال جديد" — فورم (اسم المجال، وصف مختصر، معلومات الطالب تلقائياً) يُرسل لقسم خاص بلوحة تحكم الأدمن. بعد الإرسال مباشرة، تظهر للطالب رسالة تأكيد تلقائية ("تم استلام اقتراحك بنجاح، شكراً لمساهمتك!").

### المسار الثالث: الأدمن (منفصل بالكامل)
| # | الخطوة | التفاصيل |
|---|---|---|
| 1 | دخول منفصل | صفحة برابط مستقل تماماً عن مسار الطالب، تصميم مختلف (خلفية داكنة، بدون تبويب تسجيل) |
| 2 | لوحة التحكم الرئيسية | إحصائيات عامة: عدد المستخدمين، عدد المحتوى، النشاط العام |
| 3 | إدارة المحتوى | عمليات CRUD كاملة: إضافة، تعديل، حذف، عرض |
| 4 | إدارة المستخدمين | عمليات CRUD كاملة: عرض، تعديل، حذف الحسابات |
| 5 | مراجعة اقتراحات الطلاب | عرض الاقتراحات الواردة (تاريخ، اسم الطالب، المجال المقترح، الوصف) |
| 6 | الأثر الفوري | أي تعديل يقوم به الأدمن ينعكس مباشرة على واجهة الطلاب |

## 6. مصادر المحتوى
- **فيديوهات:** YouTube Data API — عنوان، وصف، صورة مصغّرة (thumbnail)، رابط
- **مقالات:** Dev.to API — عنوان، وصف، صورة غلاف، رابط
- **الحجم المستهدف:** 80 إلى 150 عنصر، تُجلب مرة واحدة وقت الإعداد الأولي وتُخزَّن محلياً (بيانات وصفية فقط، دون تحميل الملفات الفعلية)

## 7. خوارزميات الذكاء الاصطناعي
| الخوارزمية | الدور |
|---|---|
| **Sentence Embeddings** | دمج عنوان ووصف كل محتوى في نص واحد، ثم تحويله إلى متجه رقمي يفهم المعنى دلالياً (وليس الاعتماد على العنوان فقط)، وحساب التشابه (cosine similarity) بين المحتويات |
| **K-Means Clustering** | تجميع المحتوى إلى مواضيع فرعية بعدد تجمعات (K) مُثبَّت يدوياً بين 6 و8، بدل تحديده تلقائياً — لأن حجم المحتوى المحدود يجعل طرق التحديد التلقائي (Elbow Method) غير مستقرة |

## 8. البنية التقنية
- **Next.js:** الواجهة الأمامية + API Routes + المصادقة — يغطي حوالي 90% من المشروع
- **Python (FastAPI):** خدمة مصغّرة مسؤولة عن حساب الـ Embeddings و K-Means فقط — حوالي 10% من المشروع، تتواصل مع Next.js عبر HTTP محلي على نفس الجهاز
- **SQLite:** قاعدة بيانات محلية (ملف واحد) — بدون أي Deploy، مناسبة لبيئة بدون إنترنت وسهلة النقل، وتُشحن مع المشروع معبّأة مسبقاً بالمحتوى والـ embeddings (تفاصيل بالقسم 13)
- **بدون Docker:** تشغيل مباشر عبر Node.js وPython المثبتين على الجهاز، بسكربت بدء تشغيل واحد يشغّل السيرفرين معاً — أبسط لجهاز رفيق غير تقني بدون حاجة لمفهوم الحاويات
- **التشغيل:** الإعداد الأولي الكامل (تثبيت Node.js وPython، npm install، pip install، تحميل موديل الـ Embeddings، جلب المحتوى وحساب الـ embeddings وK-Means) يتم مرة واحدة بمكان فيه إنترنت. بعدها، المشروع بالكامل — الواجهة، محرك التوصية، وقاعدة البيانات — يعمل offline بالكامل. الاستثناء الوحيد: مشاهدة المحتوى نفسه (فتح رابط يوتيوب/Dev.to الأصلي) يحتاج إنترنت وقتها لأنه بيفتح خارج المنصة

## 9. الهوية البصرية
اسم المنصة: **LearnWise**. شعار بأيقونة كتاب مدمجة بعقدة شبكة عصبية بلون تركوازي داكن (`#1D6F5C`). هوية بصرية تقنية نظيفة، ألوان محايدة مع لون مميز واحد، أيقونات outline بسيطة، بعيدة عن الطابع الزخرفي أو الكرتوني، بأسلوب SaaS تقني (مستوحى من GitHub وLinear).

## 10. النطاق، القيود، والتطوير المستقبلي

### نطاق العمل الحالي
- جلب محتوى تلقائي وتخزين بياناته الوصفية
- حساب الـ Embeddings وتجميع K-Means
- توصيات قائمة على المحتوى (Content-Based)
- Onboarding بستة أسئلة
- لوحة طالب موحدة، ولوحة أدمن كاملة (CRUD)
- ميزة اقتراح مجالات تعليمية جديدة

### القيود الحالية
- حجم المحتوى محدود (80-150 عنصر)، ما قد يقلل تنوع التوصيات
- الاعتماد الكلي على Content-Based Filtering دون Collaborative Filtering
- المجال الحالي مقتصر على البرمجة ولغات البرمجة فقط
- لا يوجد تحليل فعلي لمحتوى الفيديو أو الصوت، فقط للنص الوصفي

### التطوير المستقبلي
- تفعيل Collaborative Filtering بعد توفر قاعدة مستخدمين حقيقية كافية
- تفعيل مجالات تعليمية إضافية غير البرمجة
- تحليل الفيديو الفعلي (صوت أو صورة)
- آلية آلية لمعالجة اقتراحات الطلاب (حالياً عرض وتوثيق فقط)
- استراتيجية تصنيف المحتوى الجديد يضيفه الأدمن لاحقاً عبر "أقرب centroid موجود" (مقارنة embedding العنصر الجديد بكل centroidEmbedding مخزّن واختيار الأقرب) بدل إعادة تشغيل K-Means كاملاً في كل مرة

## 11. الجدول الزمني المقترح (12 أسبوعاً)
| المرحلة | المدة |
|---|---|
| تحليل المتطلبات وتصميم قاعدة البيانات | أسبوعان |
| بناء الواجهة الأمامية (Next.js) | 3 أسابيع |
| بناء الـ API والمصادقة | أسبوعان |
| بناء خدمة التوصية (Python) | أسبوعان |
| الربط والاختبار الشامل | أسبوعان |
| التوثيق والتجهيز للمناقشة | أسبوع واحد |

## 12. معايير التقييم والنجاح
- **اختبار دقة يدوي:** يشارك 5 إلى 10 مستخدمين تجريبيين، وكل واحد يقيّم مدى ملاءمة أول 5 توصيات ظهرت له. تُحسب نسبة الدقة = (عدد التوصيات المناسبة ÷ الإجمالي) × 100
- **جودة التجميع (K-Means):** يُحسب Silhouette Score (المستهدف: أعلى من 0.3-0.4)، بالإضافة لفحص يدوي لعينة من كل تجمّع (3-5 عناصر)
- سرعة استجابة النظام
- سهولة الاستخدام (اختبار قابلية استخدام بسيط)

## 13. التسليم والنقل لبيئة بدون إنترنت
المشروع بيوصل لصاحبه (رفيق غير تقني، بجامعة بدون إنترنت) بحزمة واحدة مضغوطة (ZIP)، مع الالتزام بالتالي:

- **التجهيز الكامل يصير قبل التسليم:** جلب المحتوى (YouTube/Dev.to)، حساب الـ embeddings، تشغيل K-Means — كل هذا يصير مرة واحدة من طرفنا، وملف SQLite النهائي (معبّأ بالكامل بالمحتوى والـ clusters) يُشحن جاهزاً جوا الـ ZIP. رفيقنا ما بيحتاج مفاتيح YouTube/Dev.to API ولا يشغّل سكربت الجلب بنفسه إطلاقاً.
- **موديل الـ Embeddings يُخزَّن محلياً جوا مجلد المشروع** (path مخصص، مو الكاش الافتراضي بمجلد المستخدم بالنظام) — هيك أول مرة يشتغّل فيها بمكان فيه إنترنت (بالبيت) بيتحمّل الموديل مرة وحدة، وبعدها بينقل مع المشروع بالضغط ومحلي بالكامل بدون أي طلب شبكة إضافي.
- **التثبيت الأولي (مرة واحدة، بمكان فيه إنترنت):**
  1. تثبيت Node.js (نسخة LTS) وPython (نسخة 3.x محددة بالـ README)
  2. فك ضغط المشروع
  3. تشغيل `npm install` بمجلد الفرونت إند
  4. تشغيل `pip install -r requirements.txt` بمجلد خدمة Python (هاي الخطوة كمان بتنزّل موديل الـ Embeddings تلقائياً بأول استخدام له)
  5. تشغيل سكربت تحقق بسيط يتأكد إنه الموديل تحمّل والـ DB موجودة وسليمة
- **التشغيل اليومي بعد هيك (بدون إنترنت):** سكربت واحد (`start.bat` على ويندوز) يفتح السيرفرين معاً (Next.js وFastAPI) بدون أي أوامر يدوية إضافية.
- **دليل تشغيل مبسّط بالعربي**، بخطوات مرقّمة وبدون مصطلحات تقنية، مرفق مع الـ ZIP (منفصل عن التوثيق التقني الأصلي).
- **اختبار قبل التسليم النهائي:** تجربة الحزمة كاملة على جهاز تاني (يفضّل بفصل الإنترنت فعلياً وقت الاختبار بعد التثبيت الأولي) للتأكد إنها تشتغل زي ما هو متوقّع من رفيقنا بالضبط.

---

# الجزء الثاني — قاعدة البيانات

قاعدة بيانات **SQLite** محلية (ملف واحد، بدون أي Deploy)، تُدار عبر **Prisma ORM**. تتكوّن من 9 جداول رئيسية + جدول وسيط واحد.

> ملف `schema.prisma` الكامل وملف ERD التفاعلي (HTML) متوفران كملفين منفصلين بنفس المشروع.

## ملخص الجداول

| الجدول | الغرض | ملاحظات رئيسية |
|---|---|---|
| **USERS** | بيانات الطالب وتفضيلاته | `initial_embedding` ثابت، `current_embedding` يتحدث بمعادلة EMA |
| **ADMINS** | حسابات الأدمن | منفصل بالكامل، بدون أي علاقة FK |
| **CONTENT** | المحتوى التعليمي (فيديو/مقال) | `embedding` من (عنوان+وصف)، `difficulty_level` و`duration_minutes` للفلترة |
| **CLUSTERS** | تجمعات K-Means (=المواضيع) | `label` فريد (UK)، `centroidEmbedding` يخزن مركز التجمّع من K-Means مباشرة، K مثبّت يدوياً بين 6-8 |
| **user_clusters** | جدول وسيط | علاقة كثير-لكثير بين USERS وCLUSTERS (اهتمامات الطالب) |
| **VIEW_HISTORY** | سجل المشاهدة | مصدر تحديث `current_embedding` |
| **SEARCH_HISTORY** | سجل البحث | — |
| **LOGIN_LOG** | سجل أوقات الدخول | — |
| **SUGGESTED_DOMAINS** | اقتراحات مجالات جديدة | عرض للأدمن فقط، بلا معالجة آلية بالنطاق الحالي |
| **CONTACT_MESSAGES** | رسائل تواصل معنا | مستقل، متاح لأي زائر بدون تسجيل |

### تفاصيل الحقول الكاملة (Prisma models)

```
User {
  id, name, email(UK), passwordHash, avatarUrl,
  level, learningGoal, learningStyle, weeklyTime, preferredContentType,
  initialEmbedding(json), currentEmbedding(json), createdAt
}

Admin {
  id, username(UK), passwordHash, createdAt
}

Content {
  id, title, description, type, source, url(UK), thumbnailUrl,
  embedding(json), difficultyLevel, durationMinutes, clusterId(FK), createdAt
}

Cluster {
  id, label(UK), centroidEmbedding(json)
}

UserCluster (join table) {
  userId(FK), clusterId(FK)
}

ViewHistory {
  id, userId(FK), contentId(FK), viewedAt
}

SearchHistory {
  id, userId(FK), queryText, searchedAt
}

LoginLog {
  id, userId(FK), loginAt
}

SuggestedDomain {
  id, userId(FK), domainName, description, submittedAt
}

ContactMessage {
  id, senderName, senderEmail, message, isRead(boolean), submittedAt
}
```

---

# الجزء الثالث — خطة التنفيذ المرحلية

التنفيذ يسير قسماً قسماً وفق نفس الترتيب دائماً: **تجهيز الباك إند أولاً ← تجهيز الفرونت إند المرتبط فيه ← اختبار القسم كاملاً ← الانتقال للقسم التالي فقط بعد التأكد من نجاح الاختبار.** لا يبدأ أي قسم جديد قبل اكتمال الذي قبله.

## المرحلة 0: الإعداد الأساسي (Foundation)
**الباك إند:**
- تهيئة مشروع Next.js (TypeScript)
- تثبيت Prisma وربطه بقاعدة SQLite محلية، وتشغيل أول migration من schema.prisma
- تهيئة مشروع FastAPI بسيط بنقطة فحص health check
- إعداد مجلد موديل محلي لخدمة الـ Embeddings (path داخل المشروع، مو الكاش الافتراضي) تمهيداً لنقل المشروع لاحقاً بدون إنترنت
- سكربت بدء تشغيل واحد (`start.bat`) يشغّل Next.js وFastAPI معاً بدون Docker

**الاختبار قبل الانتقال للمرحلة التالية:**
- التأكد من نجاح `prisma migrate dev` وظهور كل الجداول
- تشغيل السيرفرين (Next.js وFastAPI) محلياً معاً بدون أخطاء عبر سكربت البدء

## المرحلة 1: المصادقة (الطالب والأدمن)
**الباك إند:**
- API route للتسجيل (تشفير كلمة السر) وتسجيل الدخول للطالب
- API route منفصل لدخول الأدمن
- Middleware لحماية الصفحات المقفولة وتوجيه غير المسجلين

**الفرونت إند:**
- صفحة تسجيل/دخول الطالب (تصميم Split-screen)
- صفحة دخول الأدمن (تصميم منفصل، خلفية داكنة، بدون تسجيل ذاتي)
- تبديل زر Login بشريط التنقل لاسم وصورة المستخدم بعد الدخول

**الاختبار قبل الانتقال للمرحلة التالية:**
- تسجيل حساب طالب جديد وتسجيل دخول/خروج بنجاح
- التأكد أن مسار الأدمن معزول تماماً ولا يقبل حسابات الطلاب

## المرحلة 2: تحضير بيانات المحتوى (سكربت تمهيدي)
**الباك إند:**
- سكربت Python يجلب 80-150 عنصر من YouTube API وDev.to API
- حساب embedding (عنوان+وصف) لكل عنصر وتخزينه
- تحديد `difficulty_level` لكل عنصر: تحليل بسيط لكلمات مفتاحية بالعنوان/الوصف (مثل beginner/intro/101 ⇐ مبتدئ، advanced/expert ⇐ متقدم، وإلا ⇐ متوسط كافتراضي)
- استخراج `duration_minutes`: مباشرة من بيانات مدة الفيديو بـ YouTube API، أو تقدير وقت قراءة المقال من Dev.to (عدد الكلمات ÷ 200 كلمة/دقيقة تقريباً)
- تشغيل K-Means (K=6-8) وربط كل عنصر بـ cluster_id
- استخراج `cluster_centers_` من نتيجة `KMeans.fit()` (متوفرة مجاناً كناتج طبيعي للخوارزمية) وتخزين كل مركز بعمود `centroidEmbedding` بجدول Cluster المقابل
- تخزين الكل بجدولي CONTENT وCLUSTERS

**الاختبار قبل الانتقال للمرحلة التالية:**
- التأكد من امتلاء الجداول بالعدد المستهدف من العناصر
- التأكد أن كل عنصر يملك embedding وcluster_id صحيحين
- التأكد أن difficulty_level وduration_minutes معبّأين فعلياً لكل عنصر (بدون قيم فارغة)
- التأكد أن كل صف بجدول Cluster يملك centroidEmbedding غير فارغ

## المرحلة 3: Onboarding وبناء بروفايل الطالب
**الباك إند:**
- API route لحفظ إجابات الأسئلة الستة
- منطق Python لحساب initial_embedding من **4 إشارات مدمجة** (مطابقةً للتصميم بالجزء الأول، وليس المواضيع فقط):
  1. تُدمج إجابات (المستوى مع الخبرة السابقة + الهدف من التعلم + أسلوب التعلم عملي/نظري) في جملة نصية وصفية واحدة، مثال: `"مستوى متوسط، خبرة سابقة بلغة واحدة، الهدف تطوير مهارة، أسلوب تعلم عملي"`
  2. تُمرَّر هذه الجملة على **نفس** نموذج sentence-transformers المستخدم لمحتوى المنصة، لإنتاج متجه متوافق دلالياً مع باقي المتجهات
  3. يُقرأ `centroidEmbedding` المخزّن مسبقاً من جدول Cluster لكل تجمّع/موضوع اختاره الطالب بالـ Onboarding (بدون إعادة حساب)، ثم يُحسب المتوسط بينهم لو اختار أكتر من تجمّع
  4. `initial_embedding` النهائي = متوسط مرجّح بين متجه الجملة الوصفية (الخطوة 2) ومتوسط متجهات المواضيع (الخطوة 3) — مثلاً 40% جملة وصفية + 60% مواضيع، لإعطاء وزن أكبر للاهتمامات الموضوعية مع الاحتفاظ بأثر المستوى/الهدف/الأسلوب
- تخزين current_embedding كنسخة أولى من initial_embedding

**الفرونت إند:**
- معالج خطوات (Wizard) للأسئلة الستة بشريط تقدم
- شاشة تجهيز بنسبة مئوية متحركة، وزر "ادخل للمنصة" يُفعَّل عند 100%

**الاختبار قبل الانتقال للمرحلة التالية:**
- إكمال Onboarding كامل والتأكد من صحة الـ embedding المخزّن بقاعدة البيانات
- التأكد أن تغيير إجابة المستوى أو الهدف (مع تثبيت باقي الإجابات) يُنتج فعلياً initial_embedding مختلف — دليل أن الإشارات الأربع كلها مؤثرة فعلياً، مو المواضيع بس

## المرحلة 4: تصفح المحتوى (العام والمقفول)
**الباك إند:**
- API route لعرض عيّنة Teaser محدودة للزوار
- API route محمي لعرض كل المحتوى مع فلاتر (الكل / فيديو / مقال / موضوع)
- API route لتفاصيل عنصر واحد + محتوى مشابه

**الفرونت إند:**
- قسم Teaser بالصفحة الرئيسية (كروت مقفولة)
- شاشة/رسالة القفل عند محاولة دخول الكورسات بدون تسجيل
- صفحة تصفح كامل المحتوى بالفلاتر
- صفحة تفاصيل المحتوى مع رابط المصدر الأصلي

**الاختبار قبل الانتقال للمرحلة التالية:**
- التأكد أن الزائر غير المسجل يرى Teaser بس ويُعاد توجيهه عند محاولة الدخول للكورسات
- التأكد أن الطالب المسجل يرى ويتصفح كل المحتوى بحرية

## المرحلة 5: محرك التوصية
**الباك إند:**
- Endpoint بـ FastAPI (`/recommend`) يحسب التشابه (cosine similarity) ويرجع أقرب العناصر
- تطبيق **ثلاث فلاتر** على النتائج بعد الترشيح بالتشابه الدلالي: (أ) مطابقة `difficulty_level` مع `level` الطالب، (ب) `duration_minutes` مقابل الوقت المتاح أسبوعياً، (ج) نوع المحتوى المفضل
- تحديث current_embedding بمعادلة EMA بعد كل مشاهدة جديدة
- API route بـ Next.js يستدعي الـ microservice

**الفرونت إند:**
- تبويب "نظرة عامة" بلوحة الطالب: كروت التوصيات المخصصة + شرائح المواضيع (Clusters)

**الاختبار قبل الانتقال للمرحلة التالية:**
- فحص يدوي أوّلي لمنطقية التوصيات الظاهرة
- التأكد أن الفلاتر الثلاثة (المستوى، الوقت، نوع المحتوى) تعمل فعلياً وتستبعد عناصر غير مناسبة
- التأكد أن current_embedding يتغيّر فعلياً بعد كل مشاهدة جديدة

## المرحلة 6: لوحة الطالب الموحدة (سجلات وإعدادات)
**الباك إند:**
- API routes لجلب سجل المشاهدة وسجل البحث وسجل أوقات الدخول
- API route لتعديل الأفضليات الستة (وإعادة حساب initial_embedding عند التعديل)

**الفرونت إند:**
- تبويب "سجل النشاط" (مشاهدة + بحث + أوقات دخول)
- تبويب "الإعدادات" (بروفايل + تعديل الأفضليات)

**الاختبار قبل الانتقال للمرحلة التالية:**
- التأكد أن كل نشاط يُسجَّل فعلياً ويظهر صحيحاً بالتبويبات
- التأكد أن تعديل الأفضليات يُحدّث الـ embedding بشكل صحيح

## المرحلة 7: اقتراح مجال جديد
**الباك إند:**
- API route لحفظ الاقتراح في جدول SUGGESTED_DOMAINS

**الفرونت إند:**
- صفحة الفورم (اسم المجال، الوصف، معلومات تلقائية)
- رسالة تأكيد تلقائية بعد الإرسال مباشرة

**الاختبار قبل الانتقال للمرحلة التالية:**
- إرسال اقتراح تجريبي والتأكد من ظهوره لاحقاً بلوحة تحكم الأدمن

## المرحلة 8: لوحة تحكم الأدمن
**الباك إند:**
- API routes محمية بصلاحية أدمن فقط: إحصائيات عامة
- API routes لعمليات CRUD كاملة على المحتوى
- API routes لعمليات CRUD كاملة على المستخدمين
- API route لعرض اقتراحات الطلاب

**الفرونت إند:**
- لوحة تحكم الأدمن: بطاقات إحصائيات + جداول إدارة (محتوى / مستخدمين / اقتراحات)

**الاختبار قبل الانتقال للمرحلة التالية:**
- تجربة كل عمليات CRUD (إضافة/تعديل/حذف)
- التأكد أن أي تعديل من الأدمن ينعكس فوراً على واجهة الطالب

## المرحلة 9: الصفحات العامة الثابتة
**الباك إند:**
- API route لحفظ رسائل فورم "تواصل معنا"

**الفرونت إند:**
- صفحات: من نحن، الخدمات، ثقة الطلاب، الأسئلة الشائعة، تواصل معنا، الفوتر

**الاختبار قبل الانتقال للمرحلة التالية:**
- التأكد أن فورم التواصل يحفظ الرسالة بنجاح بقاعدة البيانات

## المرحلة 10: التقييم النهائي للنظام الذكي
**الباك إند:**
- تشغيل اختبار الدقة اليدوي مع 5-10 مستخدمين تجريبيين وتوثيق النسبة الناتجة
- حساب Silhouette Score على نتائج K-Means وتوثيق القيمة، مع فحص يدوي لعينة من كل تجمّع

**الاختبار قبل الانتقال للمرحلة التالية:**
- توثيق الرقمين (نسبة الدقة وSilhouette Score) بشكل جاهز للعرض أثناء المناقشة

## المرحلة 11: التغليف والتسليم النهائي (بدون إنترنت)
**الباك إند:**
- التأكد أن قاعدة البيانات النهائية معبّأة بالكامل (محتوى + embeddings + clusters) وجاهزة للشحن كملف واحد جوا المشروع
- التأكد أن موديل الـ Embeddings محفوظ بمجلد محلي داخل المشروع (مو بكاش خارجي) وجاهز للنقل معه
- كتابة `requirements.txt` و`package.json` نهائيين بنسخ محدّدة (pinned versions) لتفادي أي تعارض إصدارات عند التثبيت على جهاز رفيقنا
- سكربت بدء تشغيل نهائي (`start.bat`) + سكربت تحقق بسيط (يتأكد من وجود الموديل والـ DB قبل الإقلاع)

**التوثيق:**
- README تقني (لينا وللمشرف): خطوات التشغيل الكاملة بالتفصيل
- دليل تشغيل مبسّط بالعربي لرفيقنا غير التقني: خطوات مرقّمة بدون مصطلحات (تثبيت Node.js وPython، فك الضغط، أوامر التثبيت، تشغيل start.bat)

**الاختبار قبل التسليم النهائي:**
- تجربة الحزمة المضغوطة (ZIP) كاملة على جهاز آخر: تثبيت أولي بمكان فيه إنترنت، ثم فصل الإنترنت والتأكد أن كل شي (الواجهة، المحرك الذكي، قاعدة البيانات) يشتغل بدون أي طلب شبكة
